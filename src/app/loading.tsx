/** Soft route transition — keep chrome feel, no full-page "불러오는 중" copy */
import { RouteLoading } from "@/components/ui/route-loading";

export default function RootLoading() {
  return <RouteLoading />;
}
