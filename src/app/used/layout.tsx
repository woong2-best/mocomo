import { UsedLayoutChrome } from "@/components/used/used-layout-chrome";

export default function UsedLayout({ children }: { children: React.ReactNode }) {
  return <UsedLayoutChrome>{children}</UsedLayoutChrome>;
}
