import { WorksLayoutChrome } from "@/components/layout/section-layout-chrome";

export default function WorksLayout({ children }: { children: React.ReactNode }) {
  return <WorksLayoutChrome>{children}</WorksLayoutChrome>;
}
