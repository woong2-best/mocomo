"use client";

import { AppPageChrome } from "@/components/layout/app-page-chrome";

export function SettingsPageChrome({ children }: { children: React.ReactNode }) {
  return <AppPageChrome>{children}</AppPageChrome>;
}
